
PostgreSQL

docker run -it --rm \
  -e POSTGRES_USER="root" \
  -e POSTGRES_PASSWORD="root" \
  -e POSTGRES_DB="taxi" \
  -v postgres_data:/var/lib/postgresql \
  -p 5432:5432 \
  --network=postgres_pgAdmin_python \
  --name pgdatabase \
  postgres:18

pgadmin

docker run -it \
  -e PGADMIN_DEFAULT_EMAIL="admin@admin.com" \
  -e PGADMIN_DEFAULT_PASSWORD="root" \
  -v pgadmin_data:/var/lib/pgadmin \
  -p 8085:80 \
  --network=postgres_pgAdmin_python \
  --name pgadmin \
  dpage/pgadmin4


Docker file

docker run -it \
  --network=postgres_pgAdmin_python\
    python_scripts\
    --pg-user=root \
    --pg-pass=root \
    --pg-host=pgdatabase \
    --pg-port=5432 \
    --pg-db=taxi 